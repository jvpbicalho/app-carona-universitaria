/// <reference types="expo/types" />

// Gerado pelo Expo. Declara os tipos das variáveis EXPO_PUBLIC_* em
// process.env; sem ele, process.env vira `any` e o tsc perde a inferência
// (e, junto, o noImplicitAny em quem consome essas variáveis).
