# --- Instruções internas do Claude Code (não versionar) ---
CLAUDE.md
.claude/

# --- Node ---
node_modules/
coverage/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tsbuildinfo

# --- Expo / React Native ---
.expo/
.expo-shared/
dist/
web-build/
android/
ios/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# --- Variáveis de ambiente (contêm chaves) ---
# .env.example NÃO entra aqui: é o molde versionado que a equipe copia para
# criar o próprio .env, e só tem placeholder.
.env
.env.local
.env.*.local

# --- Supabase CLI ---
supabase/.branches/
supabase/.temp/
.supabase/

# --- Editores / SO ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# Bloco gerenciado pelo próprio Expo (o `expo start` o recria). O
# expo-env.d.ts é regenerado a cada execução; a referência de tipos que o
# typecheck precisa num clone novo fica em src/types/expo.d.ts, versionado.
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
